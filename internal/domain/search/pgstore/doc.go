/*
Package pgstore persists search documents in PostgreSQL.

The package stores denormalized search rows and serves read-side search queries
with PostgreSQL full-text search plus indexed filters.
*/
package pgstore
