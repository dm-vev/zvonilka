/*
Package search owns the messenger search index and query model.

The package stores denormalized search documents for users, conversations,
messages, and media, then resolves read-side search hits from those documents.
*/
package search
