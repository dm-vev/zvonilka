/*
Package pgstore implements the bot store on top of PostgreSQL.

The package keeps webhook configuration, queued updates, and worker cursors
aligned with the bot domain store contract.
*/
package pgstore
