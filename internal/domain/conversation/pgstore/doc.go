/*
Package pgstore implements the conversation store on top of PostgreSQL.

The package keeps conversations, messages, attachments, read-state, delivery
events, and sync cursors aligned with the domain store contract.
*/
package pgstore
