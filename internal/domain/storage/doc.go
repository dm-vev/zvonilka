/*
Package storage provides the universal storage contracts used across the project.

It defines provider metadata, a catalog for multiple backends, and a transaction
helper that lets domain services model atomic write units without binding
themselves to a concrete driver.
*/
package storage
